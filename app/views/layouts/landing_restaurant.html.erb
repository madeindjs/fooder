<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render 'layouts/meta', cache: true %>
    <%= csrf_meta_tags %>
    <!-- Bootstrap core CSS -->
    <% if @restaurant && @restaurant.css %>
      <%= stylesheet_link_tag @restaurant.css %>
    <% else %>
      <%= stylesheet_link_tag 'https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css' %>
    <% end %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    <style type="text/css">
      div.jumbotron.header{margin-bottom: 0; border: none;}
    </style>
  </head>
  <body style="margin-left: 0;margin-rigth: 0;margin-bottom: 0;margin-top: 40px;">
    <div class="container-fluid">
      <%= render 'layouts/navigation', cache: true %>
      <div class="row">
        <%= yield %>
      </div>
    </div>
    <%= render 'layouts/footer', cached: true %>
  </body>
</html>