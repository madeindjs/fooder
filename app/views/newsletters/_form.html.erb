<%= bootstrap_form_for newsletter, layout: :horizontal, label_col: "hidden", control_col: "col-sm-12" do |f| %>
  <%= render 'shared/errors_form', model: newsletter %>
  <%= f.text_field :email, hide_label: true, placeholder: "Votre email" %>
  <%= f.submit "S'abonner" %>
<% end %>
