<h1>Actualité</h1>


<% @posts.each do |post| %>

  <article>
    <%= image_tag post.picture.thumb, class: 'img-circle section-img' if post.picture? %>
    <h2><%= link_to post.title, restaurant_post_path(@restaurant, post) %></h2>
    
    <p><%= post.user.complete_name %> dans <%= tags post.tags %></p>
    <p class="text-justify"><%= post.content %></p></p>

    <!-- buttons -->
    <p class="text-right">
      <%= link_to "voir l'article", restaurant_post_path(@restaurant, post), class: 'btn btn-primary' %>

      <% if check_admin %>
        <%= link_to 'editer', edit_restaurant_post_path(@restaurant, post), class: "btn btn-default" %>
        <%= link_to 'Supprimer', restaurant_post_path(@restaurant, post), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'  %>
      <% end %>
    </a></p>
  </article>

<% end %>

<% if check_admin %>
  <%= link_to 'Ajouter un article', new_restaurant_post_path(@restaurant), class: 'btn btn-primary' %>
<% end %>


