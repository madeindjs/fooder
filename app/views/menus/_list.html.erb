<% menus.each do |menu| %>
  <div class="col-xs-12 col-md-6 col-lg-4">
    <h3><%= menu.name %> <span class="price">24 €</span></h3>
    <p class="text-justify"><%= menu.description %></p>


    <ul class="list-unstyled">
    <% menu.dishes.each do |dish| %>
      <li>
        <strong><%= dish.name %><br/>
        <%= dish.description %>
      </li>
    <% end %>
    </ul>

<% end %>