<%= bootstrap_form_for link do |f| %>
  <%= render 'shared/errors_form', model: link %>

  <%= f.text_field :name, label: 'Nom' %>

  <%= f.select  :link_type_id,
      options_for_select(LinkType.all.map{ |p| [ p.name , p.id] }),
      {label: 'Type de lien', help: "Précharge le logo"} %>

  <%= f.text_field :url, label: 'Lien' %>

  <%= render 'shared/form_input_logo.html', form: f, model: link %>

  <%= f.submit 'Confirmer' %>
<% end %>
