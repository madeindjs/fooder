<div class="jumbotron">
  <h1>Editez vos Catégories</h1>
  <p>Ici éditez / réorganisez rapidement vos catégories. Vous pouvez accéder au formulaire complet en cliquant sur <i>En savoir plus</i></p>
</div>



<%= form_tag categories_edit_path do  %>

  <div class="sortable">

    <% @categories.order(:order).each_with_index do |category, order| %>

      <div id="<%= category.id %>" class="item col-xs-12">

        <div class="col-xs-1 handle">
          <span class="glyphicon glyphicon-sort"></span>
        </div>

        <%= hidden_field_tag "categories[#{category.id}][order]", order, class: 'order' %>

        <div class="col-xs-7">
          <%= text_field_tag "categories[#{category.id}][name]" , category.name, class: 'form-control' %>
        </div>

        <div class="col-xs-2">
          <%= label_tag "menu[#{menu.id}][activate]" do %>
            <%= check_box_tag "menu[#{menu.id}][activate]", 1, menu.activate %> Activer
          <% end %>
        </div>


        <div class="col-xs-2">
          <div class="btn-group-vertical" role="group" aria-label="...">
            <%= link_to 'en voir plus' , edit_category_path(category) , class: 'btn btn-xs btn btn-default' %>
            <%= link_to 'supprimer', category, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-xs btn-danger' %>
          </div>
        </div>

      </div>

    <% end %>
    
  </div><!--/ .sortable -->

  <%= submit_tag 'Tout mettre à jour', class: 'btn btn-primary' %>

<% end %>

